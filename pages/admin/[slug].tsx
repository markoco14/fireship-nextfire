import MetaTags from "../../components/Metatags"

export default function AdminPostEdit({ }) {
    return (
        <main>
            <MetaTags title="admin page" description="so good" image="null"/>
            <h1>You are slugging it!</h1>
        </main>
    )
}
